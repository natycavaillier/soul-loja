import { ObjsEmailPipe } from './objs-email.pipe';

describe('ObjsEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjsEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
